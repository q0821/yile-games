# 變更紀錄（Changelog）

本檔案記錄專案的重要變更。
版本標識沿用 app 的慣例：`vYYYY.MM.DD-<commit>`（build 時自動產生，顯示於頁尾）。

## v2026.07.04 — 新增「連六棋」（Connect6）

### 新增
- **連六棋（Connect6）**：交大吳毅成發明、用圍棋棋具玩的棋種。19 路盤，黑方第一手下一子、之後雙方每手各下兩子，先在任一方向連成六子者勝——「每手兩子」自然抵銷先行優勢，比五子棋更公平。支援人機對弈（電腦難度易／中／難）與雙人對局，含悔棋（以整回合為單位）、落子與勝負音效、水墨風封面。

### 修正
- 修正各棋種在電腦思考中離開畫面、稍後再返回時可能卡住不動的問題（五子棋、黑白棋、連六棋）。
- 修正對弈畫面在部分情況下版面錯位（棋盤擠成細長條）的問題。

## v2026.07.02 — 全棋種音效與音樂、AI 建議走法補齊、版面一致化

### 新增
- **全棋種音效**：六種棋（含死活、殘局練習）落子、吃子、終局都有對應質感的音效——圍棋玻璃子、象棋木子拍擊、將棋駒音、西洋棋木子輕放、黑白棋翻子聲。
- **將軍語音**：象棋「將軍！／絕殺！」（中文）、將棋「王手！／詰み！」（日文）、西洋棋 "Check! / Checkmate!"（英文），將死時也有對應語音（認輸、和局不播）。
- **背景音樂**：兩首古琴環境氛圍曲隨機輪播、曲間淡入淡出。預設關閉，可在設定開啟。
- **全域音訊設定**：首頁與每個棋種的設定內都有同一份「音效／音樂開關與音量」，改一處全站同步、立即生效。
- **建議走法補齊到象棋、將棋、西洋棋**（原本僅圍棋有）：按下後引擎以完整棋力算一手，棋盤上畫箭頭提示；將棋「打入」以目的格＋持駒高亮呈現。
- **圍棋禁著點即時回饋**：點到已有子、自殺手、打劫禁著點時，該點閃紅 X＋文字說明＋提示音，不再無聲無息像當機；劫爭中的禁著點會常駐標記。五子棋、黑白棋的無效點擊也補上提示。
- **虛手預警**：任一方虛手後提示「再虛手一次將進入數目」。
- **全新 APP icon**：書法「弈」字＋朱砂印章，宣紙底，取代先前的預設圖示。

### 變更
- **六棋版面一致化**：統一「標題列＋資訊列＋棋盤＋狀態列＋常駐功能列」結構。象棋／西洋棋資訊列顯示手數與雙方損子、黑白棋子數移入資訊列；覆盤按鈕常駐顯示（終局後可按）；五子棋、黑白棋的設定改為與其他棋一致的「設定」彈窗。
- **畫面質感精修**：棋盤加木紋質感、盤側厚度與桌面投影，棋子光澤與淺浮雕、落子動畫與提子淡出，按鈕與彈窗層次打磨。所有動效尊重系統「減少動態效果」設定。
- **支援 iPhone 動態島／瀏海**：畫面頂部不再被遮擋（safe-area 支援，直向橫向皆處理）。

### 修正
- **取消數目後單次虛手即被判終局**：現在取消數目返回對局後，需重新累積兩次連續虛手才會終局。
- 建議走法在快速操作下的狀態競態、將棋打入建議的座標格式、音效載入首次失敗後永久靜音、背景音樂錯誤處理與來電中斷後的恢復。

## v2026.06.10 — 分頁標題依棋種變化、流量追蹤準備

### 變更
- **瀏覽器分頁標題會隨目前頁面變化**（如「象棋對弈 · 弈樂」「圍棋死活 · 弈樂」），切到首頁回復站名。多分頁時更好辨識，對搜尋與分享也更友善。
- 預先接好流量分析的換頁事件：切換棋種時送出一次瀏覽計數（站方在 Cloudflare 設好分析工具後即生效；未設定時不影響站台、不送任何資料）。

## v2026.06.10 — 象棋／將棋／西洋棋：電腦難度可自動調整

### 新增
- **難度自動調整**（象棋、將棋、西洋棋；人機對局）：在設定開啟後，**電腦連敗 2 盤自動升一級、連勝 2 盤降一級**（和棋或一勝一負則維持），讓棋力慢慢貼近你的程度。對局結束畫面會提示升降，設定面板顯示目前等級並可一鍵「重設等級」。
- 等級改為連續多級（取代內部固定三段）；手動「簡單／普通／困難」仍保留，對應到固定等級。可隨時關掉自動、退回手動選難度。
- **設定收進「設定」按鈕**（象棋／將棋／西洋棋，比照圍棋）：對弈模式、執子、難度、自動調整、目前等級等偶爾才調的選項，收進標題列右上的「設定」彈窗；對弈中常用的悔棋／重新開始／覆盤等仍留在棋盤下方。畫面更清爽、各棋類體驗一致。

## v2026.06.10 — 將棋、西洋棋新增 AI 覆盤

### 新增
- **將棋、西洋棋對局結束後可「覆盤檢討」**（原本僅圍棋、象棋有）：以滿血引擎逐手分析，畫出**優勢曲線**（先手／白方視角）、可用滑桿或前後鍵逐手回放，每手標示佳著／失誤／大失誤與失分、並提示該手的**最佳著法**。點曲線可直接跳到該手。
  - 將棋覆盤期間隱藏持駒區（盤面位置仍精準）；盤上不畫最佳變化箭頭（打入無起點難表示），最佳手以文字呈現。

### 修正
- **覆盤：被將死的致勝手不再被誤判為「大失誤」**。終局（無合法手）局面引擎不回傳評分，原會被當成均勢、連帶讓將死那一手算成大失誤；現正確視為「輪到的一方必敗」，將死手標為佳著、終局顯示勝勢。此修正同步套用於象棋、將棋、西洋棋覆盤。

## v2026.06.10 — 象棋／將棋／西洋棋 AI 改為主動棋風

### 變更
- **AI 不再「防守強、進攻軟」**：原本靠 `UCI_LimitStrength` 降棋力，會在候選著法裡刻意挑次佳手——而保持攻勢的常是唯一的最佳手，於是攻擊機會被一手一手讓掉。改為**全力引擎＋限制搜尋深度**，AI 永遠下牠在該深度看得到的最佳手（含棄子搶攻、當頭炮等主動著法），棋風更自然、有企圖心。
- **每局不再千篇一律**：簡單／普通難度會在「接近最佳」的著法間隨機選一手，同一開局也會有不同變化；困難維持引擎最佳手。
- 三段難度改以搜尋深度區分（簡單淺算、困難深算），手感更平順。
- 此調整由象棋、將棋、西洋棋**共用引擎**，三種棋的電腦對手同步受惠。

## v2026.06.09 — 分享卡片與站台描述

### 新增
- 補上 SEO `meta description`、`canonical`、**Open Graph** 與 **Twitter／X 卡片**：分享到 FB／LINE／Threads／X 時會帶標題、描述與縮圖（弈樂「弈」字）。

### 變更
- 站台標題改「弈樂 · 多棋類線上對弈」；PWA `manifest` 名稱／描述更新為涵蓋全部棋種。
- 瀏覽器主題色（`theme-color`／manifest 顏色）由舊深色 `#1a1a2e` 改為暖色，與宣紙主題一致。

## v2026.06.09 — 新增西洋棋

### 新增
- **西洋棋**人機／雙人對局：8×8 暖色棋盤格、水墨剪影棋子（黑方深墨、白方象牙底＋描邊）。
  與電腦或雙人對弈，難度三段；含合法手提示、最後手標記、將軍提示、AI 思考延遲、悔棋、
  與其他棋一致的結束畫面、走子滑動動畫。
- **升變**：兵抵底排跳「后／車／象／馬」四選一。**王車易位**、**吃過路兵**、將死／逼和判定皆由引擎處理。
- 棋規與合法手、AI 沿用 Fairy-Stockfish／ffish（與象棋、將棋共用引擎，變體 `chess`），詳見「關於與授權」。
- 首頁改為上 4 下 4 兩列（依棋種分組）。

## v2026.06.09 — 界面打磨：圍棋版面、棋子、首頁、控制列

### 變更
- **圍棋改為單欄置中版面**（對齊其他棋類）：遊戲設定收進「設定」彈窗；對弈功能（虛手／悔棋／申請數目／認輸／提示／覆盤）常駐盤下功能列、**不再收進選單**；桌機與手機同一套響應式版面。
- **圍棋、五子棋改用立體棋子**（與黑白棋一致的暖色立體黑白石，抽出共用繪製）。
- **首頁卡片**改為依棋種分組的網格、卡片描述改為意象對句、卡片放大。
- 對弈棋盤下的控制鍵**依性質分組**（行棋／提示／終局／重置），「重新開始」位置各棋一致。
- 修正 App 圖示「弈」字偏下；iOS 圖示改為滿版（避免透明角被填黑）。

## v2026.06.09 — 新增日本將棋

### 新增
- **日本將棋**人機／雙人對局：9×9 暖色格盤、漢字楔形駒（先手正向、後手 180° 倒置、升變駒紅字）。
  與電腦或雙人對弈，難度三段；含合法手提示、最後手標記、王手提示、AI 思考延遲與讀取效果、悔棋、
  與其他棋一致的結束畫面、走子滑動動畫。
- **升變（成る）**：駒走入敵陣可升變時跳「成る／不成」選擇，強制升變自動處理。
- **持駒與打入**：吃到的駒收入持駒區（盤上、盤下各一排顯示與計數），可點選持駒打回盤上空格；
  二步、打步詰等限制由引擎處理。
- **規則說明**：內建將棋規則速覽彈窗（駒走法、升變、打入、目標），方便不熟將棋的玩家。
- 棋規與合法手、AI 皆沿用 Fairy-Stockfish／ffish（與象棋共用引擎，變體 `shogi`），詳見「關於與授權」。

## v2026.06.09 — 更新 App 圖示（弈樂「弈」字）

### 變更
- App 圖示／favicon 由圍棋風改為品牌「弈」字（暖色宣紙底），不再偏單一棋種。

## v2026.06.09 — 新增象棋殘局；棋盤補兵卒/炮位標線

### 新增
- **象棋殘局**：古典殘局練習（基本殺法、江湖殘局、夢入神機、適情雅趣，共 1107 題）。
  執先手方求殺取勝，引擎當對手防守到底並判定成敗；可看提示（建議走法）、上/下/隨機/重置題目；
  走子有滑動動畫、已解題會記錄並顯示「已解 N」。題庫取自棋弈江湖（MIT），詳見「關於與授權」。

### 變更
- **象棋棋盤補上兵卒位、炮位的傳統直角標線**（象棋對弈與殘局共用）。
- 首頁排序調整為：圍棋／死活、象棋／殘局、五子棋／黑白棋。

## v2026.06.09 — 新增黑白棋（翻轉棋）

### 新增
- **黑白棋**人機／雙人對局：8×8 暖色盤、黑白立體棋子，落子夾翻、子多者勝。
- 自製 AI（位置權重 + 機動性 + alpha-beta），難度三段；含合法手提示、計分、
  自動跳過（無合法手）、AI 思考延遲＋讀取效果、悔棋、與其他棋一致的結束畫面。
- **翻子動畫**：被夾的棋子翻轉換色，落子帶 pop-in。

### 新增
- **象棋覆盤**：對局結束後可進入覆盤，用滑桿／上一手／下一手逐手回看整局。
- **分析本局**：按下後用引擎逐手評估，顯示**每個局面的優劣**（紅優／黑優 +X）、
  **這手的好壞分類**（佳著／正常／小失誤／失誤／大失誤）、**丟了幾分**，以及**最佳手**。
- **優勢曲線圖**：整局評估畫成曲線（紅優在上、黑優在下），點曲線可跳到該手。
- **最佳變化預想**：盤上以藍色箭頭標出該局面的最佳續著（前幾手），看出該怎麼走較好。
- 都來自 Fairy-Stockfish 真實評估，不做棋理臆測。

## v2026.06.08 — 結束畫面一致化（圍棋／五子棋／象棋）

### 變更
- 三種棋的對局結束畫面統一視覺：暖色卡片 + 宋體結果標題 + 「再來一局」+ 暗化模糊背景。
- **五子棋新增結束覆蓋卡片**（原本只有狀態文字）。
- 圍棋結果彈窗對齊同風格，保留比分摘要與「去覆盤」。

## v2026.06.08 — 象棋對弈體驗優化

### 變更
- **棋子立體實體感**：凸面漸層 + 投影 + 描邊高光；加大棋盤內距，邊角棋子不再被切。
- **AI 思考 1–3 秒並顯示讀取效果**，不再瞬間落子。
- **棋子移動動畫**：看得到棋子從哪走到哪。
- **將軍提示**（紅色「將軍」橫幅 + 將帥高亮）與**明顯的結束畫面**。

### 新增
- **悔棋**：人機對局可退回你可下的時機（連電腦那手一起退）。

## v2026.06.08 — 新增象棋對弈

### 新增
- **中國象棋**人機／雙人對局：AI 用 Fairy-Stockfish（WASM 引擎）、棋規用 ffish。
- 楚河漢界棋盤、選取與合法點提示；難度三段（以引擎 UCI_Elo 調整棋力）。

## v2026.06.08 — 新增五子棋對弈

### 新增
- **五子棋**人機／雙人對局：自由連五，先連成一線者勝。
- 威脅優先 + 連子型態啟發式 AI，難度三段；含悔棋。

## v2026.06.08 — 改名「弈樂」、標題改用系統宋體

### 變更
- 站名由「圍棋對弈」改為「**弈樂**」，涵蓋圍棋、死活練習、五子棋、象棋等多種棋類。
- 標題與介面改用系統內建宋體呈現，移除外連 Google Fonts（讓象棋引擎能在含 iOS Safari 的全瀏覽器啟用）。

## v2026.06.08 — AI 出錯不再卡死、不再「幫 AI 下子」

### 修正
- **AI 發生錯誤後整局卡死**：對弈中只要 AI 推論偶發失敗一次（如 WebGPU device lost／worker 推論錯誤），原本就直接停手、只能開新局；而且「開始新遊戲」對「引擎已壞」其實無效，往往得整頁重整才會好。
- **出錯後點棋盤會「幫 AI 下子」**：錯誤發生後仍停在 AI（白）回合，但棋盤點擊只靠 `isAIThinking` 旗標擋你落子；旗標被錯誤清掉後，點棋盤就以白棋幫 AI 下、把棋局弄亂。

### 變更
- **自動重試恢復**：AI 每手失敗會先就地重試（重試前重置引擎）；整輪仍失敗會延遲後自動再算一輪。多數偶發失敗會自動恢復、**不丟失當前對局**。
- **引擎可重置**：新增引擎 `reset()`（terminate 壞掉的 worker、清掉 client singleton），讓「重試／開始新遊戲」能真正重建乾淨引擎恢復，毋需整頁重整。
- **回合防護**：人 vs 電腦只在「輪到你」時才允許手動落子（以回合狀態判斷，不再只靠旗標），杜絕幫 AI 下子。
- **顯示真正錯誤**：狀態列改為顯示 AI 真正的錯誤訊息，不再只說「發生錯誤」，便於定位根因。

## v2026.06.06 — 建議走法加上「為什麼」（數據＋預想圖）

### 新增
- **建議走法附說明**：按「建議走法」時，除了藍圈「薦」標出該下的點，狀態列會顯示**這手值幾目**（KataGo 真實領先目數，換成你的視角），盤上並畫出**後續變化預想圖**——藍線連起「你下這、對方應這、你再下…」並標序號，用實際變化解釋為什麼這手好。
- 都來自 KataGo 真實輸出（領先目數、PV 主要變化），不做棋理唬爛。

## v2026.06.06 — 新增「建議走法」提示（用 KataGo）

### 新增
- **建議走法（AI）**：對弈中輪到你時可按，用 KataGo 算出「這手該下哪」，在盤上以藍圈「薦」標出。這是真正的「下一手提示」。
- 原本的「提示」其實只是**吃子提示**（標出能直接提子的點），大多數時候沒有可吃的子所以看似沒反應——按鈕已更名為「吃子提示」以免混淆，並與新的「建議走法」並列。

## v2026.06.06 — 對弈自適應難度（階梯式 + 約業餘級位）

### 新增
- **電腦難度依戰績自動升降**：取代原本固定的「低/中/高」三段。每局結束依**你贏幾目**調整——贏 ≥ 10 目升一級、小贏原地、輸 ≥ 20 目才降一級（階梯式，傾向往上爬，當作棋力進度指標）。
- **明示升降**：對局結束彈窗會顯示「電腦升到第 N 級」等訊息。
- **約業餘級位估計**：等級旁標示「約 N 級」（估計值、非正式定級，僅供參考）。
- **低等級隨機弱化**：低級時電腦會從 KataGo 候選手中故意挑差一點的手（用引擎的 pointsLost 控制差幾目），讓初學者也贏得了、能一路往上升；高級則只下最佳手、全力搜尋。
- 等級存 localStorage 跨次保留；設定面板可「重設等級」。

## v2026.06.06 — 標題恢復毛筆字（內嵌，不再消失）

### 變更
- **首頁標題改回毛筆書法字**（先前因「圖被 CDN 快取成 HTML 導致標題消失」而暫時降級為宋體）。
- 根治做法：把毛筆字圖改成 **base64 data URI 內嵌**（CSS 與水墨動畫共用），隨檔案一起送達、**不發外部圖片請求、CDN 碰不到**，所以標題永遠是毛筆字且不可能消失。
- 仍保留純文字當極端 fallback；水墨揭示動畫照常。
- 附 `scripts/inline-title.sh`：日後更換標題圖後重跑即可重新內嵌。

## v2026.06.06 — 數目改用 KataGo（修正勝負相反）

### 修正
- **數目結果可能與實際相反**：對局（尤其未完全收完官子時）按「申請數目」會把整片棋誤判成死子清掉，導致一方盤面變 0 目、勝負相反（曾出現「白方一大片地卻判黑勝」）。
- 根因：對弈引擎已升級 KataGo，但數目仍用舊的純 JS 死子估算（`estimateDeadStones`），在未終局盤面會「雪崩式」誤殺整片棋。
- 修法：數目時改用 **KataGo 的 ownership**（每個交叉點屬黑/屬白的判定）推導死子與領地，這正是專業圍棋 app 的做法，未終局也準確得多。KataGo 載入失敗時自動退回舊估算。

## v2026.06.06 — 讓子對局（S6）

### 新增
- **讓子（handicap）**：「人 vs 電腦」設定新增「讓子」選項（2–9 子）。選了之後你固定執黑、先在標準星位佈下讓子，電腦執白先下、白方貼 0.5 目。讓越多子越好打——讓初學者也能和強很多的 KataGo 下出有來有往、有指導性的對局。各棋盤大小（9/13/19 路）都用對應的標準星位。

### 說明
- 讓子局結束後一樣可數目；計分已套用 0.5 貼目。匯出的 SGF 會正確帶上 `HA`／`AB`（讓子標記與佈子），其他軟體可正常開啟。

### 新增
- **無障礙（S11）**：對弈與死活的狀態列加上 `aria-live`，螢幕報讀軟體會即時播報「換誰下、AI 思考中、勝負結果、死活對錯」等狀態；棋盤、計時器補上 `aria-label`。

### 變更
- **移除 GnuGo 引擎**：自從換上更強的 KataGo（可當對手又能誠實分析）後，舊的 GnuGo（約 5–6 級、`score()` 失效）已無用武之地。移除整套 GnuGo 引擎與 7.1MB 的 `gnugo.wasm`，AI 對手與分析全部交給 KataGo（其內建 WebGPU→WASM→CPU 後端自動切換，毋需另一套引擎兜底）。對弈、覆盤、SGF 匯出功能不變。

### 說明
- 不再支援的「離線純 GnuGo」情境：KataGo 模型約一次性下載 3.8MB（會被瀏覽器快取），首次需要 AI 時才載入，純對弈/死活認形不受影響。

## v2026.06.05 — 計時器改 wall-clock（S12）

### 修正
- **計時器背景分頁漂移**：舊版用 `setInterval` 每跳一次減 1 秒，但瀏覽器會把背景分頁的 `setInterval` 節流到約每分鐘一次，導致切到別的分頁再回來時，時鐘「慢了」（漏算了沒跳到的秒數）。改為 **wall-clock**：記錄每手開始的時間戳與剩餘秒數，依「真實流逝時間」回推剩餘，tick 只負責刷新顯示與檢查超時。即使分頁在背景被節流，回到分頁、換手或結束時都會用真實時間精確校正（實測背景分頁誤差 < 0.2 秒）。對局介面與設定不變。

## v2026.06.05 — 死活：正解後續手 play-out（S7）

死活練習原本只判第一手關鍵點，學不到「為什麼這手對、對手反抗怎麼應」。整合 KataGo 後補上後續手驗證。

### 新增
- **「試著走完」**：在死活題解對第一手後出現。按下後可繼續在局部與 KataGo 對走（玩家走解題方、KataGo 當對手只在局部範圍應手），畫面持續以**領地覆蓋層（陰影深＝黑、淺＝白）**顯示 KataGo 對盤面的歸屬估計，讓你親眼看目標棋串到底活還死。隨時可「停手」還原到解對第一手。

### 說明
- **刻意不顯示「勝率 %」或「成功/失敗」二元判定**：死活盤面 99% 是空的，KataGo 的整盤勝率由搶空盤大場主導、與這個角落死活幾乎無關（黑解對了也可能顯示黑 3%），硬秀會嚴重誤導；而局部領地加總又會被「對方圍住的牆」帶偏。唯一誠實又有意義的訊號是**逐點領地覆蓋層**本身——與覆盤領地圖同源，不替引擎硬下它給不出的精確結論。此選擇延續本專案「寧可不秀，也不秀會誤導的數字」原則（同形勢曲線/數目估算被砍的理由）。
- **AI 局部選手改用 policy**（修正「按了不會接著下完」）：原以 KataGo 搜尋出的候選手清單挑局部手，但近乎空盤時 visits 都花在滿盤大場、候選常整批落在局部範圍外 → 多數題一按就立刻收手、走不下去。改用 dense 的 policy（每點都有先驗值）在局部範圍挑最高合法點，保證有手且與強手一致。另加收手門檻：局部底定（policy 掉到雜訊水準）才停，避免在空處補無意義填子；「一手即定」的題會老實回報「沒有需要驗證的後續手」。

## v2026.06.04 — 更清楚的結束對局方式

對弈結束方式原本只有「認輸」最明顯，想贏要靠「雙方連續虛手 → 數目」，但很多人不知道，導致贏了卻不知怎麼收尾。

### 新增
- **「結束對局（數目）」按鈕**：對弈中隨時可按，直接進入數目、算出誰贏幾目，不必猜虛手機制。桌機與手機操作列都有。
- **AI 虛手提示**：當 AI 虛手（pass）時，狀態列明確提示「AI 虛手了 — 你也虛手即可數目，或按『結束對局』直接計算結果」。

### 說明
- AI（GnuGo）本身**不會主動認輸**——它的形勢計算在此 WASM 版本失效，無法可靠判斷該不該投降，硬做會誤判，故不採用。改以上述「玩家主動結束 + 數目」的誠實方式判定勝負。

## v2026.06.03 — 移除低價值的 AI 輔助

把「靠不太強的 GnuGo、容易誤導或養成依賴」的功能砍掉，讓 AI 回歸單純當對手；學習交給之後的死活題庫。

### 移除
- **覆盤 AI 分析**：它只能說「你這手跟 AI 同不同」，而 GnuGo 棋力不高（約 5–6 級），這資訊幾乎沒有學習價值。連同分析面板、進度條一併移除。**覆盤本身保留**（可逐手回看自己的棋）。
- **新手引導**：對弈中即時標出 AI 建議點，等於教人模仿不強的 AI、且會讓人不思考。移除。

### 保留
- 覆盤（純回看）、顯示氣數、實戰即時提醒（叫吃警告）、吃子提示——這些都是純規則、客觀、對初學有用的輔助。

## v2026.06.02 — 覆盤分析改為誠實做法

### 變更
- **重做覆盤評分**：舊版會替每手標「✅好手／約損失 X 目」，但這建立在 GnuGo 失效的形勢計算（實測回傳空字串）與不可靠的中盤領地估算上，導致中盤幾乎每手都判好手、開局形勢曲線亂跳——明明輸了卻「全好手」。現在改成只用 GnuGo 唯一可靠的訊號（它的推薦手）：每手標示「✅ 和 AI 同手」或「🔍 與 AI 不同（附 AI 會下的位置，不代表你這手不好）」。
- **移除形勢曲線**：它依賴失效的形勢估算，數字會誤導，故移除。
- **即時提醒改為叫吃警告**：不再謊報「損失 X 目」，改成偵測「你這手下完後，自己這塊只剩 1 氣（快被吃）」——這是用規則就能 100% 確定的危險訊號。

### 修正
- **手機按「AI 分析」沒反應**：按下後立即顯示進度（0%）並自動捲動到分析面板，確保看得到分析正在進行。

## v2026.05.31 — 手機版覆盤體驗

### 新增
- **結束對局彈窗**：對局結束時，跳出的視窗新增「🎮 再來一局」與「📖 去覆盤」按鈕，不必再回設定選單。
- **AI 思考停頓**：AI 回手前會停頓隨機 1–3 秒（GnuGo 實際算棋時間計入其中），不再瞬間落子，讓你能從容思考。

### 修正
- **手機看不到覆盤分析結果**：好手/惡手統計、形勢曲線、每手評語原本只放在右側資訊面板，而手機版會把該面板隱藏，導致覆盤時只看得到上一手/下一手箭頭。現在進入覆盤時會把分析面板移到棋盤下方（手機可見），並在覆盤導覽列直接提供「🤖 AI 分析」「❌ 結束覆盤」按鈕。

## v2026.05.31 — 修正：氣數顯示

### 修正
- **棋子表情在手機上不顯示**：原本用 emoji（😰😐🙂😄）畫在 canvas 上，但彩色 emoji 字型在行動瀏覽器（尤其 iOS Safari）的 canvas 渲染並不可靠，導致棋子上看不到任何符號。改為**直接用 canvas 繪製「氣數」數字**，不依賴 emoji 字型，並依危險程度上色（1 氣紅、2 橘、3 黃、4+ 綠）。設定項更名為「顯示氣數」。

---

## v2026.05.30 — 學習模式（提升棋力）

新增一整套幫助玩家**提升自己棋力**的學習功能。AI 引擎維持 GnuGo 不變，重點在「用 AI 當練習與檢討工具」。

### 新增
- **更準的覆盤評分**：覆盤分析改用「**失目**」評分（<2 目=✅好手、2–6=⚠️疑問手、>6=❌惡手），取代舊版以「離 AI 建議多遠」的曼哈頓距離判斷。逐手顯示「約損失 X 目」與「AI 建議：○○」。
- **形勢曲線圖**：分析後在覆盤面板畫出整盤領先目數折線（中線為均勢），一眼看出逆轉手；**點曲線可直接跳到該手**，惡手以紅點標示。
- **實戰即時提醒**：設定可開關（預設關）。人機對局走出明顯壞手時，跳出非阻斷式提示「約損失 X 目，AI 會下在 ○○」並附「重下」鈕，背景執行不卡 AI。
- **重下練習迴圈**：覆盤時可「↻ 從這手重下」分支出新對局換個下法續弈，「↩️ 返回原譜」隨時還原，不破壞原始棋譜。
- **`LEARNING.md`**：學習功能說明與搭配的訓練方法。
- 新增單元測試 `tests/learning.test.js`（形勢估計、失目計算、分級門檻）。

### 修正
- 修復覆盤分析會崩潰的問題：`estimateBlackLead` 原本傳 `null` 給會被迭代的 `deadStones` 參數，導致 `calculateScore` 拋出例外。

### 技術備註
- 形勢分數改用 app 內建的領地計算（純 JS）估算，而非 GnuGo 的 `score` 函式——實測發現此 WASM 建置版本的 `score()` 回傳空字串、無法使用。失目為「玩家實際走法」與「GnuGo 建議走法」兩者下完後的形勢差，越接近終局越準。
- 測試：132 個全數通過；Vite production build 通過。

---

## 先前版本（節錄既有 git 歷史）

- **build 修正**：調整 build 順序，先產生版本號再 `vite build`；改用 `Date.now` 防快取以修復版本號不更新。
- **部署**：以自訂 Dockerfile（Caddy 服務 `dist/`）繞過 Zeabur builder 問題。
- **介面**：改善文字清晰度、統一按鈕用語；修復手機版棋子表情不顯示。
- **功能**：棋子表情模式（氣數視覺化）。
